{
  "name": "weather_dashboard",
  "version": "1.0.0",
  "description": "A simple dashboard that shows the weather in one or multiple locations.",
  "main": "index.js",
  "scripts": {
    "build": "npm install && node create-env.js && cd netlify/functions && npm install",
    "postbuild": "echo 'Build completed'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv": "^16.4.5",
    "http-proxy-middleware": "^3.0.2"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
